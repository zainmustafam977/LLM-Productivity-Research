# Tutorial for using an IDE with a conversational AI
def average(lst):
    if len(lst) == 0:
        return 0
    return sum(lst) / len(lst)
    
# Example usage:
numbers = [5, 5, 5, 5, 5]
print(average(numbers))  # Output: 5.0


def calculation(list):
    
    return

print(average(numbers))
print(calculation(numbers))